/*
All these tests for code coverage run are now done with tunnel.check
tests. Individual tests for tunnel/options can still be done.
*/
/*describe('Options', function() {
  require('./tunnel/options')
})*/

describe('Binary', function() {
  require('./tunnel/binary')
})

describe('Process', function() {
  require('./tunnel/process')
})
